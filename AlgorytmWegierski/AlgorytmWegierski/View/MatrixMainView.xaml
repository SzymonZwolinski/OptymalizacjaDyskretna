<Window x:Class="AlgorytmWegierski.View.MatrixMainView"  
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlgorytmWegierski.View"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <ListView Name="MatrixGrid"  ItemsSource="{Binding Matrix}">
            <ListView.ItemTemplate>
                <DataTemplate>

                    
                        <TextBlock Grid.Row="{Binding RowId}" Grid.Column="{Binding ColumnId}" Text="{Binding Number}"></TextBlock>
                 

                </DataTemplate>
            </ListView.ItemTemplate>



             
            
            
            
        </ListView>
        
    </Grid>


</Window>  